import styled from "styled-components";

export const ProductSec = styled.section`
  h4.head {
    border-left: 5px solid var(--main_color);
  }
  .lds-ripple {
    transform: unset !important;
  }
`;
